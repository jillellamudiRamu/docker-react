sudo: required
services:
  - docker


before_install:
  - docker build -t jillellamudiRamu/docker-react -f Dockerfile.dev .
script:
 - docker run jillellamudiRamu/docker-react npm run test -- --coverage